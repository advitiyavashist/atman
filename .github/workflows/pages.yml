name: pages

on:
  push:
    branches: [main]
    paths:
      - landing/**
      - docs/assets/demo/**
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Copy published demo assets into the Pages artifact
        run: |
          mkdir -p landing/assets/demo
          cp docs/assets/demo/hero.gif landing/assets/demo/hero.gif
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: landing
      - id: deployment
        uses: actions/deploy-pages@v4
