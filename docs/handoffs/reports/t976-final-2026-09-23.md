# T-976 final-candidate journey run — 2026-09-23

**Verdict: PARTIAL.** Live Codex directed-message wake and same-provider
interruption/continuation now pass. Fresh source installation supplies the
React app. Cross-provider continuation is NOT RUN: a second provider could
not authenticate in the required temporary HOME. The live scratch worker also
encountered a missing-origin sync refusal. This is not an all-exit-steps PASS.

## Candidate and isolation

Fresh GitHub depth-1 clone of `advitiyavashist/atman`, exact main
`c1f88d11bd8f91bd30b44971746abedfe7fbae60`. macOS arm64; Python 3.9.6;
Node 24.7.0. `./install.sh --prefix /tmp/atman-t976-0923-bin` ran with temporary
HOME and built `ui/dist` successfully without a separate UI build or author
intervention. `atm self` from the real worker confirms the fresh clone's root
`tickets.py` and both installed aliases. No production source changed.

All journey mutations used the CLI against throwaway boards. Inherited
TICKET/ATMAN/CLAUDE/CODEX/CURSOR environment variables were removed. Scripted
probes disabled usage refresh and carried a test guard. Live probes removed
that guard and used the installed Codex executable with a private temporary
copy of the existing login; no credentials are included in evidence. Provider
settings were isolated from the operator's HOME. Temporary sources, boards,
credentials, venv, browser dependency directory and processes were removed
after evidence capture. No Linux or distribution-upgrade certification.

The committed evidence is redacted, not rewritten: each run's private
temporary root appears as `<tmp>`, the two probe scripts resolve the operator's
`~/.local/bin` through `os.path.expanduser` instead of a literal home, and the
local git committer identity in the provider transcript appears as
`<operator> <operator@host>`. Nothing else in the receipts was altered.

## Results

| Exit surface | Result and evidence boundary |
| --- | --- |
| Fresh source-prefix install | PASS; installer itself built `/app/` |
| Normal objective/dependency/claim/message/artifact/review/accept | PASS for CLI mechanics only, with stub seats: committed exact JSON independently read, self-accept refused, dependency blocked until separate-seat accept. The closing `atm done` and the `git merge --ff-only` were evaluator commands typed against the disposable board and repository, not a product merge/close path, so this row is NOT evidence for the "merged and marked done" exit clause |
| Live directed wake | PASS; real Codex watcher triggered on directed message and returned unique ACK |
| Interrupted live worker, same provider | PASS for artifact continuation and stable ownership: the live worker committed stage one, was terminated, and a fresh invocation of the SAME registered `live-codex` seat completed stage two and submitted review. The ticket was never reopened and owner/`owner_generation` never changed, so this row is NOT evidence for the "reopened and inherited" exit clause, and not cross-provider recovery |
| Live worker sync | REFUSED as designed: scratch repository had no origin; provider reported this and still submitted review. No claim that the complete sync path passed |
| Cross-provider continuation | NOT RUN; isolated Claude auth reported false and Cursor reported Not logged in |
| Adverse setup and coordinator interruption | PASS for bounded scripted cases listed below |
| Fresh-install app desktop/mobile | PASS at 1440 and 390 pixels; HTTP 200, no page errors or overflow |
| Mobile intervention and ticket details | PASS; message survived reload and full review SHA displayed |
| Targeted regression suites | PASS, 51 tests |

### Live wake and continuation

The watcher was enrolled as `live-codex`, harness `codex`, continuous wake,
with no open ticket. The coordinator sent nonce `t976-0923-wake`; the CLI
reported `watch-poked`, the watcher recorded `messages_to_me/task_messages`,
and the real provider called `atm inbox` and posted `ACK t976-0923-wake` to
boss. This is an actual provider receipt, not a queued-mail or registration
claim. The default adapter ran `codex exec`; no stub or custom harness ran
these live steps. Three bounded provider invocations covered one short ticket
plus the wake receipt; no additional provider workers were dispatched.

The next invocation claimed the single two-stage ticket, committed `mid.txt`
with exactly `stage-one\n` at `4071ed0`, and posted the artifact and exact next
action. The evaluator sent SIGTERM to the watcher process group after that
durable note; Atman's supervisor terminated its active child. The supervisor
itself exited 0 during orderly shutdown, which is not a successful completed
worker result. The provider had not created `finished.txt` or submitted review.
This proves process interruption after a real intermediate artifact, not a
native conversation-resume claim.

A fresh invocation of the same registered Codex seat consumed the persisted
claim/note, preserved `mid.txt`, wrote `finished.txt`, asserted the exact bytes,
committed and submitted review at
`a829f64e28d94fcd3ee98929564c1fdc574a98bc`. The evaluator independently read
`git show <sha>:finished.txt`, confirmed `stage-one\nstage-two\n`, checked
unchanged owner and generation, and recorded acceptance as the separate Bob
reviewer identity. Bob's independent check was the evaluator's deterministic
assertion, not a second model review. `live-result.json` preserves the receipt.

The worker's `atm sync` refused because this local scratch repository had no
remote. It reported that limitation to boss; its subsequent review succeeded.
That is preserved in `live-provider.log`, not hidden as a passing sync. The
missing remote is a probe setup limitation, not evidence of a remote-sync
regression. The production report branch separately runs sync before review.

A second provider was sought without changing the operator's configuration:
Claude and Cursor binaries were installed, but their existing local settings
copied into temporary HOME did not supply usable authentication. No credential
values were printed. The planner received a stuck message and progress note;
no auth workaround, new login, or cross-provider claim was invented.

### Repeated mechanics and adverse probes

The normal artifact SHA is `6cf57003447931cf822f50ee8aeca2389700507b`.
Independent JSON assertions checked rows=3 and columns=2; author self-accept
and premature dependent claim were refused. Independent acceptance released
the dependency, then the disposable main was fast-forwarded.

The scripted killed-process recovery also passed, as did: missing binary,
missing auth, stub-auth dispatch, concurrent claim (one winner), repeated
claim (generation unchanged), failed-check rejection, owner transfer fencing,
foreign/stale executable overwrite refusal with recovery guidance, and killed
custom coordinator watcher restart. These remain **scripted runtime evidence**;
provider labels in these probes are not live interchangeability evidence.
Stale-install coverage is foreign executable protection, not an old release
upgrade. Corrected prior probe argument mistakes were used from the outset.

### App and regression evidence

The fresh install served `/` and `/app/` at both 1440×900 and 390×900, with
no horizontal overflow or browser page errors. Screenshots are included.
The operator selected Alice as lead, posted a mobile message, reloaded, and
saw it persist. Ticket drilldown showed the full normal artifact SHA. The
message to offline Alice is queued-mail evidence only; the separate live
Codex receipt above proves wake. All 17 README relative Markdown paths exist;
remote links and anchors were not revalidated.

An initial test invocation found no system pytest; an isolated temporary
venv supplied pytest. The fresh pinned source then ran:

```text
python -m pytest -q tests/test_t977_recovery_contract.py \
  tests/test_t944_accept_reject.py tests/test_t1077_quickstart.py
51 passed in 131.84s
```

No full-suite rerun. One UI probe initially omitted the documented `lead set`
subcommand; correcting that probe invocation enabled the interaction check.
These are evaluator setup corrections, not production changes.

## Handoff

[Evidence directory](t976-final-evidence/) includes command receipts, adapted
probe scripts, provider transcript, pre/post interruption ticket records,
install/test logs, browser assertions and screenshots.

No Atman production source was changed by this run, but it was not a
hands-off run and is not offered as one. The evaluator acted as coordinator
throughout: its scripts wrote and committed the scripted-journey artifacts,
drove the CLI under the `alice`, `bob` and `boss` identities, accepted as
`bob`, issued the closing `atm done` and `git merge --ff-only` by hand, and
sent the live Codex seat a steering message naming `mid.txt` and the next
action to continue from. The live seat's two commits and its review
submission are its own; the coordinator's other steps are not.

Retain PARTIAL and the README's narrowed cross-provider promise;
remaining acceptance work is an authenticated second-provider continuation
and a remote-backed live sync journey if required as an exit step. Do not
reinterpret the successful same-provider run as cross-provider recovery.
