T-002 is claimed and implemented on this worktree. I did not mark it complete.

**Handoff SHA, verified on T-001:** `103e2249eeb4d2bf2ccd094b710f78f5cb890168`  
`atm show T-001` has the same accept SHA. Fast-forward merge in this worktree succeeded (`2d3a2f2` → `103e224`).

**`summarize("data/sales.csv")`:**
```
{'north': {'units': 15, 'revenue': Decimal('600.00')}, 'south': {'units': 7, 'revenue': Decimal('315.50')}, 'east': {'units': 21, 'revenue': Decimal('987.25')}}
```

**B:** `saleskit/cli.py` `report` uses that `summarize()` function. CLI test is in `tests/test_cli.py`.

**Tests:** `python3 -m unittest discover -s tests` — 5 tests, OK.

**CLI:**
```
north 15 600.00
south 7 315.50
east 21 987.25
```

**Commit:** `cursor-worker-work@310f465df378725a71a88ac19857f45bc2779ac2` — `saleskit/cli.py`, `tests/test_cli.py`. Stopped here; T-002 stays in progress.
