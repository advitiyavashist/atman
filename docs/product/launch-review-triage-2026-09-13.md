# Launch review triage

T-886 · strategic review of current board reports · 2026-09-13

The immediate objective is an executable first-user loop: connect agents with distinct identities, plan real work, submit evidence for independent review, and release dependent work after acceptance. The public app and documentation must describe that tested behavior. This is guidance for CEO and CoS, not another engineering or merge approval hop.

## Help required now

| Area | Current evidence | Decision / next action | Existing owner lane |
| --- | --- | --- | --- |
| Agent identity and restart | T-839 resubmitted PR #116 at `2a942f0`; author reports a live worker still inherited the coordinator's `TICKET_SEAT`. T-875 PR #119 `07072ae` repairs lost spawn config and caller-worktree reporting. | Test the actual child launch, not just sanitized unit-test environments: worker agent and seat must both be unique, parent provider/session identity absent, worker worktree correct, coordinator unchanged. Verify current integrated heads through T-876 and spawn acceptance; older T-847 acceptance of `7fbae42` cannot accept the new delta. | CEO/CoS route backend and independent reviewers. |
| Global Codex hook installer | T-873 removed the live global registrations; T-856 owner explicitly says installer recurrence requires a residual. | T-887 created for scoped installer defaults and source/installed-package negative tests. Keep the local repair separate from framework completion. | New bounded backend residual; CoS staffs a distinct worker. |
| Unattended messages | T-857 `c9f19e2` is in FIX; T-885 waits for replacement. CEO's live protocol evidence says the injector socket does not ACK. | A successful write is delivered-unconfirmed, never proof of wake. Reuse stable message ID across connection-error retries; do not retry after successful write. Independent reviewer already has CEO live evidence and needs the new SHA, not another socket or another protocol debate. Run-level receiver ACK/work evidence remains separate. | Existing author/reviewer; one replacement pin is the unblock. |
| First-run task graph | T-868 QA reproduced capture-lane B after A completion. T-879 PR #121 `13cefa0` was rejected for fabricated sounding placeholders. | Preserve capture-to-sound gate. Plan fields must contain real cause/change/proof; the README's A/B example includes them. Show capture status separately from unfinished dependency status. A ready B is distinct from actual B execution and from accepted completion. | T-879 + T-827; QA reruns literal README after repair. |
| Local versus GitHub review | T-880: documented review command rejects sounded code without a PR; direct done bypasses the advertised independent gate. | Recommend an explicit per-board local review mode for first-time/local users, with branch/SHA-bound evidence and a distinct reviewer; GitHub review remains an option. Do not add force-as-approval or silently close code work. Until the mode exists, document the real PR prerequisite rather than suggesting an invented command. CEO chooses the mode contract before backend implements. | Existing T-880; product decision, not a new rewrite. |
| App readiness and health | T-884: nonexistent compose ticket, setup shown complete without a coordinator, contradictory attention labels. T-810 remains unclaimed and blocked by CLI acceptance. | Never default compose to a nonexistent ticket. Derive one health status from actual blockers. Derive setup completion from the user's chosen operating mode: coordinator required in coordinated mode, not CEO/CoS titles in every team. Separate setup from tested live wake. Reserve a UI worker for T-810/T-884; do not launch a second app. | Existing UI tickets; T-863 shares this surface. |
| Requested quota onboarding | T-860 design `c51952d` is still in review; T-862/T-863 are capture and blocked on acceptance. | CEO disposition of the existing design unlocks backend/UI planning in parallel. CoS sound real cause/change/proof, staff unique workers, reuse T-687 auth and T-810 app. Unknown quota is not exhausted quota. Alert exact provider-setting changes when automatic configuration is unavailable. | Existing graph; no duplicate onboarding tickets. |

## Product and documentation corrections

Atman T-869's useful findings belong in T-819: correct the join name/role syntax; workers submit review rather than close their own code tickets; remove private team restrictions from public CLI defaults and primary docs navigation; show interactive versus supervised execution and real provider-specific connection commands. Keep historical operational evidence outside the primary path rather than deleting other teams' work.

Do not adopt the audit's proposed permanent provider tiers from this board's temporary subscription restrictions. Any-agent participation and verified unattended wake are separate capability rows. Do not claim learned cost reductions or universally automatic worktree creation. Keep `atm` primary after installed-package acceptance, with `tickets` compatibility, rather than reversing the operator's naming decision because an older audit saw packaging pending. First-run board connection should show and confirm an ambient override; clearing cross-repo board environment globally would break valid existing sessions (T-878).

Steer T-870's strongest findings belong in T-823: a runnable SG advertisement pack request and a structured tool-boundary example; explicit pack version/applicability/facts/receipt documentation; a brief action-to-agent loop; and separate DLP examples. Use canonical fixture payloads and actual supported transport. Show the result in the same example, including who must apply block or hold review. Do not assert cryptographic receipt authenticity from a digest-only contract.

One audit recommendation is wrong and would cause avoidable API churn. Both SDKs on Steer origin/main implement `judge` for HTTP `POST /v1/evaluate`: Python `sdk/python/steer_dlp/__init__.py` lines 121–125; TypeScript `sdk/typescript/src/index.ts` lines 77–78. Their `evaluate` method calls offline dataset `POST /v1/evaluations` (Python lines 192–195, TypeScript line 109). The landing's `steer.judge` is therefore not an invented method. Keep the runtime route primary, use actual SDK initialization/signatures, and do not replace it with the dataset method. Pack examples should use HTTP when SDK pack-field support has not been demonstrated.

The frozen T-813 registry lists all three packs as sandbox/unsigned, not only UK. Each public activation claim needs its own counsel/lifecycle state and T-814/T-854/T-815 measured verdict. Keep the recommendation pack's no-overall-allow rule. Model-assisted signals do not independently block. No pack-level compliance guarantee or NER-default change follows from a passed corpus review.

## Metrics and review efficiency

T-874 now verifies the corrected scorecard; do not interrupt with another speculative metrics rewrite. Supply only the existing nine KPI contracts: exact eligible denominator, frozen as-of replay, input digests, null for unknowns, all coordination/failed-attempt cost, and independent outcome evidence. That reviewer must also name the real artifact repository: the board identifies a Steer PR for an Atman metric deliverable. T-831 must publish an Atman-owned accepted result; a cross-repo board number alone is not sufficient provenance.

Stop counting idle broadcasts, inbox appearance, watcher start or capture-lane release as task success. One owner, one current candidate pin, one independent verdict, one accepted merge is the engineering end state; setup/user-test acceptance has its own measured endpoint. Coordinator actions should name the next dependency they unlock. Stale leadership-transfer planning tickets should be reconciled by CEO as historical/superseded, not reopened as another leadership change.

This review is based on board audit reports and targeted source inspection, not new full-suite, browser or live-wake testing. No implementation claim or merge decision is made here.
