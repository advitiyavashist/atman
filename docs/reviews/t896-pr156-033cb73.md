# T-896 independent review: Atman app PR #156

Verdict: **FIX** at `033cb738ecba2d552304ea2e7a2d46c30b9df476`.

The app behavior and evidence model are intact. The remaining launch fixes are
limited to the top-level connection label and mobile information density.

## Evidence

- Focused product and contract tests: 89 passed. The first sandboxed run passed
  87 tests and could not bind localhost for two server tests; those two passed
  separately with localhost permission.
- Independent headless Chrome run against a throwaway board at 390x844 dark and
  1280x1000 light: no horizontal overflow and no page error. Objective, Done
  when, Finishing, Blocked, Next step, graph controls, and the dependency graph
  rendered from the real `/board.json` payload.
- At 1280px the vertical order was objective at 276px, summary at 410px, and
  graph at 694px. At 390px those same sections began at 597px, 750px, and
  1360px. The summary exists but the user must pass the product header, status
  chips, warning, action strip, long objective strip, onboarding strip, and tabs
  before reaching it.
- The exact candidate still maps a successful board refresh to the word
  `connected` in `tickets.py`. That proves the page reached the board server;
  it does not prove an agent executable is authenticated or responding.
- The focused suite covers keyboard toggle semantics, initial selected-work
  propagation, mobile jump/compose binding, separate inbox-read and wake
  receipts, exact-artifact review state, and the same-second reopen boundary.

## Required fixes

1. Render the page transport state as **Board synced**. Reserve connected,
   authenticated, and responding for agent-specific states backed by their own
   evidence.
2. At 390px, collapse the duplicated status chrome so the Objective and at
   least the start of Finishing / Blocked / Next step are visible in the first
   viewport. Keep the data reachable in an existing details surface; do not
   remove evidence or redesign the Work view.

Re-run the focused T-810/T-889/T-884/T-791/T-323/T-944 set and one 390px browser
probe. No full-suite repeat is needed for these two presentation changes.
