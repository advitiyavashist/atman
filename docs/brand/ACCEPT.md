# ACCEPT stamp — T-347

**CEO ACCEPT** already granted on T-346 / [PR #4](https://github.com/advitiyavashist/atman/pull/4) (Atman brand direction + TEAM IA). T-347 ships the locked mark and tokens. Do not merge PR #4 from this ticket.

Locked by that ACCEPT:

| | |
|---|---|
| Mark Week-1 | **A — formation dots** (3–5 dots, Total Football constellation). Hold B. C only if A fails 16px. **No edges.** |
| Name | Soft-lock **Atman**. Chrome wordmark `atman` (lowercase + tracking). Not `ATMAN`. Domain HOLD — no invented URLs. |
| Tokens | T-276 dark roots. `--intervene` / warn amber `#e0a53d` for Intervene CTAs. `--live:#3ee8c5` sparse pulse / live-seat only. Do not replace intervene amber with mint. |
| Out of scope | `tickets.py` `UI_HTML` (T-345). Steer `^`, pale lime, lock/shield, robot, neon, external assets, build step. |

**T-345 implementer = `cursor-atman-ui`.** ui lane = craft / a11y eyes only.

Paste-ready `:root` and `--live` / `--intervene` rules: [atman-tokens.md](atman-tokens.md).
Mark: [assets/mark.svg](assets/mark.svg).
