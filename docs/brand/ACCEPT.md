# ACCEPT stamp — brand lock

Locked brand decisions for Atman V1:

| | |
|---|---|
| Mark | **Formation dots** (3–5 dots, Total Football constellation). **No edges.** |
| Name | **Atman**. Chrome wordmark `atman` (lowercase + tracking). Not `ATMAN`. |
| Tokens | Dark roots `#0c0e12`. `--intervene` / warn amber `#e0a53d` for Intervene CTAs. `--live:#3ee8c5` sparse pulse / live-seat only. Do not replace intervene amber with mint. |
| Out of scope | External asset hosts, lock/shield chrome, robot motifs, neon glow fields, build-step asset pipelines. |

Paste-ready `:root` and `--live` / `--intervene` rules: [atman-tokens.md](atman-tokens.md).
Mark: [assets/mark.svg](assets/mark.svg).
